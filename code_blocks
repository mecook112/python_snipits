# List comprehension

# Instead of doing:
squares = []
for i in range(10):
  squares.append(i*i)
  
# do this instead
squares = [i*i for i in range(10)]

valid_choices = [str(i) for i in range(1, len(available_parts) + 1)

# Indexing
itemList = [
            'item 1',
            'item 2',
            'item 3',
            'item 4'
            ]
for number, part in enumerate(itemList):
  print("{0}: {1}".format(number + 1, part))
